'use client';

// PostHog temporarily disabled for launch - will re-enable after proper testing
export function CSPostHogProvider({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
