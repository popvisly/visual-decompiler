import * as Sentry from "@sentry/nextjs";

// Sentry temporarily disabled for launch - will re-enable after proper testing
// Sentry.init({
//     dsn: process.env.NEXT_PUBLIC_SENTRY_DSN,
//     tracesSampleRate: 1.0,
//     debug: false,
// });
